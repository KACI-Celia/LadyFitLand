{% extends 'base.html.twig' %}

{% block title %}Afficher les espaces{% endblock %}

{% block body %}
    <h1>Afficher les espaces</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ espace.id }}</td>
            </tr>
            <tr>
                <th>LibelleEspace</th>
                <td>{{ espace.libelleEspace }}</td>
            </tr>
            <tr>
                <th>DescriptionEspace</th>
                <td>{{ espace.descriptionEspace }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-dark" href="{{ path('app_espaces_admin_index') }}">retour</a>

    <a class="btn btn-warning" href="{{ path('app_espaces_admin_edit', {'id': espace.id}) }}">Modifier</a>

    {{ include('espaces_admin/_delete_form.html.twig') }}
{% endblock %}
